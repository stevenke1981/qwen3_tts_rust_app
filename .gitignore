/target
*.wav
*.mp3
qwen-tts.toml
models/
qwentts.cpp/
# Keep example config but ignore active config
qwen-tts.toml
# Tooling artifacts
.opencode/tantivy/
.opencode/memory.db*
.opencode/vectors.usearch

# Local tooling (protoc, etc.)
.local/

# Workspace tooling artifacts
${PROJECT_ROOT}/

# qwen shared library and ggml runtime DLLs
*.dll
!src/**/*.rs
